# カタンの点数計算とサイコロ（catan）

Claude.aiチャット「カタン用ボードゲームツール開発」で作った単一HTMLを、2026-09-05に統括セッションが受け入れたもの。

- 公開先: https://catan.kkpwebninja.com/（未公開・これから）
- 本体: `index.html` 1ファイル完結。ノービルド。
- 中身: 初期配置バー・モード切替・サイコロ・全員の点数・出目パッド。スマホ1画面に収める設計。
- 得点も出目も保存しない（リロードで消える）。1ゲームぶんのメモという位置づけ。

## 設計で触ってはいけないところ

- **スマホ1画面完結**。毎ターン触るもの（サイコロ・点数・出目パッド）がスクロールなしで届く状態を保つ。
  セクションを足すときは、必ずツールより下に置く。
- **押せるものは物理的に「浮かせる」**。押せる／押せないが色や枠の太さだけで区別されていると迷うため、
  影で浮きを付けて統一してある。新しいボタンも同じ扱いにする。
- 6と8、2と12が隣り合わない初期配置生成ロジック。

## KKP共通ルール（受け入れ時に整備 2026-09-05）

| 項目 | 値・方針 |
|---|---|
| デプロイ | **Cloudflare Pages + GitHub Actions**（`.github/workflows/deploy.yml`）。GitHub Pagesは使わない |
| GA4 | `G-2LM85GJN0L`（設置済み） |
| AdSense | `ca-pub-1298304917726270`（設置済み・自動広告はOFF運用） |
| 広告位置 | ツール直後（使い方の前）と、本文末尾（共有の前）の2枠。`.ad-slot`＋「広告」ラベル＋高さ予約320x100/728x90。**位置の追加・移動はユーザー確認必須** |
| アフィリ | Amazon `kkp15-22` ／ 楽天 `11008fdf...`。いずれも**検索リンク方式**（存在しないASINを指さないため）。PR表記必須 |
| 著者名 | JSON-LDの`author`/`publisher`は **「web忍者の砦」固定**。個人名「こーきっぺ」は絶対に入れない |
| PP | 共通PP https://privacypolicy.kkpwebninja.com へリンク（新規作成しない） |
| フッター | `WEBNINJA_UNIFIED_FOOTER` の統一版 |
| 関連リンク | `WEBNINJA_RELATED_APPS`。ボドゲ系クラスタ（ito / gesture / karuta / morse） |
| OGP | 1200x630 の `ogp.png` をルートに置く。SNS共有URLには `?d=YYYYMMDD` を付ける |

## 残タスク

1. **`ogp.png`（1200x630）の作成** — `<meta property="og:image">` が参照済みだが実体がまだない
2. GitHubリポ作成 → Cloudflare Pages → DNS CNAME → カスタムドメイン（`new-ninja-site` スキルで一気通貫）
3. 楽天アフィリの**サイト登録**（新規サブドメインは登録が要る）
4. 公開後: apex（kkpwebninja.com）への掲載・sitemap更新は**別タスク**
5. GSCへのサイトマップ登録

## 非公式ツールであること

『カタン』のファンが作った非公式ツール。権利は各権利者に帰属する旨をフッターに明記してある。
公式にない設定を作らない・数で公式を上回る書き方をしない、という砦の方針を守ること。
